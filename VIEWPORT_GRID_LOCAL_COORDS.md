# 0.1.5 栅格与局部坐标系修复

- 栅格改为根据相机视口动态生成，始终铺满当前 VTK 显示区域。
- 缩放/平移后按当前世界坐标重新生成栅格，主/次栅格自动调整间距。
- 全局坐标系的 X=0 / Y=0 轴随视口延伸，线宽高于普通栅格。
- 局部坐标系模式不改变任何 VTK Actor 的全局位置，只叠加 24 个振镜区域边界及每区局部 X/Y 轴。
- 24 振镜固定布局在应用启动时由 Rust `get_galvo_layout` 提供，因此即使尚未打开数据集，也可以切换并查看局部坐标区域。
- `适应视图` 不再把有限栅格纳入 camera bounds，而是按 24 振镜实际覆盖区域计算。
